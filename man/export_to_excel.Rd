% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_results.R
\name{export_to_excel}
\alias{export_to_excel}
\title{Export check results to an Excel workbook (AfCEST professional data quality report format)}
\usage{
export_to_excel(
  report,
  data = NULL,
  id_col = NULL,
  path = file.path(path.expand("~/Downloads"), "hfc_report.xlsx"),
  enum_col = NULL,
  date_col = NULL,
  keep_cols = NULL,
  sheets = "all",
  backcheck_data = NULL,
  ...
)
}
\arguments{
\item{report}{An \code{adc_report} object (from \code{\link[=run_all_checks]{run_all_checks()}}) or a
list of \code{check_result} objects.}

\item{data}{Optional original data frame. When provided, category sheets
include full data rows for flagged records.}

\item{id_col}{Character. ID column name in \code{data}.}

\item{path}{Character. Output file path (\code{.xlsx}). Defaults to
\verb{~/Downloads/hfc_report.xlsx}.}

\item{enum_col}{Character. Optional enumerator column name. When provided,
adds an "Enumerator Dashboard" sheet.}

\item{date_col}{Character. Optional date column name. When provided,
includes submission dates in relevant sheets.}

\item{keep_cols}{Character vector of additional column names from \code{data} to
include in the Flagged Records output (e.g., village name, phone number).
These columns help field teams identify and locate respondents. Columns
that do not exist in \code{data} are silently skipped with a warning.}

\item{sheets}{Character. Which sheets to include. \code{"all"} (default) includes
all 18 sheets. A character vector (e.g., \code{c("summary", "corrections")})
includes only those sheets plus README. A negative character vector
(e.g., \code{c("-fabrication", "-back_check")}) excludes those from all.}

\item{backcheck_data}{Optional data frame with back-check comparison data.}

\item{...}{Reserved for future use.}
}
\value{
Invisible path to the created file.
}
\description{
Creates a multi-sheet Excel workbook in AfCEST professional
data quality report format with up to 18 sheets covering
README, summary, duplicate IDs, outliers, logic checks, constraints,
other-specify, missing data, skip patterns, survey tracking, GPS issues,
timing issues, fabrication indicators, enumerator dashboard, flagged
records, corrections template, back-check results, and field comments.
}
